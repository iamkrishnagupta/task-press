<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome to BE-7!</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <h2 class="text-center">Hi, Welcome back!</h2>

    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="outline-none rounded-lg block px-5 py-2 bg-zinc-700 w-full"
            type="text"
            placeholder="Title goes here..."
            name="title"
          />
          <textarea
            class="rounded-lg outline-none w-full block px-5 py-2 bg-zinc-700 resize-none mt-3"
            placeholder="Write your task here"
            name="details"
            cols="30"
            rows="10"
          ></textarea>
          <input
            class="rounded-lg mt-2 bg-zinc-800 p-2"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-4 flex-wrap">
        <% if(files.length > 0){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 p-4 bg-zinc-800 rounded-lg">
          <h1 class="text-white text-2xl tracking-tighter"><%= val.split('.txt').join('') %></h1>
          <div class="flex w-full justify-between items-center">
            <a class="text-zinc-400 inline-block mt-2" href="/file/<%=val%>">read more.</a>       
            <a class="text-zinc-400 inline-block mt-2" href="/edit/<%=val%>">edit title?</a>
          </div>

        </div>
        <% }); %> <% } else { %>
        <h3 class="text-zinc-300">oops! no tasks yet!</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
